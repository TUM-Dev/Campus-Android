<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/widget_margin"
    android:background="@android:color/white">

    <TextView
        android:id="@+id/mensa_widget_header"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:minHeight="40dp"
        android:textSize="20sp"
        android:typeface="sans"
        android:textColor="#ff656865"
        android:padding="8dp"
        android:layout_alignParentTop="true" />

    <ImageView
        android:id="@+id/mensa_widget_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_cutlery"
        android:layout_toRightOf="@id/mensa_widget_header"
        android:padding="8dp" />

    <ListView
        android:id="@+id/food_item"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp"
        android:layout_below="@id/mensa_widget_header" />

    <TextView xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/empty_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/food_item"
        android:gravity="center"
        android:textStyle="bold"
        android:textColor="#ff656865"
        android:text="No more item found!"
        android:textSize="20sp" />
</RelativeLayout>