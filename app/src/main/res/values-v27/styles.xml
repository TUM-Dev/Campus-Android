<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/tum_light_gray</item>
        <item name="colorPrimaryDark">@color/default_window_background</item>
        <item name="colorAccent">@color/tum_blue</item>
        <item name="android:windowBackground">@color/default_window_background</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>
        <item name="android:textCursorDrawable">@drawable/cursor_text_field_colorful</item>

        <!-- Transparent status bar -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>

        <!-- Light navigation bar -->
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:navigationBarColor">@color/default_window_background</item>
        <item name="android:navigationBarDividerColor">@color/list_divider</item>

        <item name="android:listDivider">@color/list_divider</item>
    </style>
</resources>